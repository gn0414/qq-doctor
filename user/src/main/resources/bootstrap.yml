spring:
  application:
    name: user-server
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        # 命名空间
        file-extension: yaml
        namespace: qq-doctor-dev
        group: user
      discovery:
        namespace: qq-doctor-dev
        group: user
