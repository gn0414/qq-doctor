spring:
  application:
    name: message-server
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        # 命名空间
        file-extension: yaml
        namespace: qq-doctor-dev
        group: message
      discovery:
        namespace: qq-doctor-dev
        group: message
