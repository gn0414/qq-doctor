spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: 10.21.91.245:8848
        # 命名空间
        file-extension: yaml
        namespace: qq-doctor-dev
        group: gateway
      discovery:
        server-addr: 10.21.91.245:8848
        namespace: qq-doctor-dev
        group: gateway
    gateway: #网关配置
      discovery: #服务发现
        locator: #资源定位
          enabled: true #通过服务发现和查找其他微服务
