spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        # 命名空间
        file-extension: yaml
        namespace: qq-doctor-dev
        group: gateway
      discovery:
        namespace: qq-doctor-dev
        group: gateway
