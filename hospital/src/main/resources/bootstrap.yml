spring:
  application:
    name: hospital-server
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        # 命名空间
        file-extension: yaml
        namespace: qq-doctor-dev
        group: hospital
      discovery:
        namespace: qq-doctor-dev
        group: hospital
